def get_server_from_token(token):
    """Extract server region from JWT token safely"""
    try:
        decoded = jwt.decode(token, options={"verify_signature": False})
        lock_region = decoded.get("lock_region", "IND")
        return lock_region.upper()
    except Exception:
        return "IND"
